/**
 * Les primitives du chrome, partagées par `apps/demo` et `apps/design`.
 *
 * La feuille qui les habille n'est pas réexportée ici — elle s'importe par le
 * sous-chemin `@defsquare/data-graph-chrome/chrome.css`, et APRÈS les variables
 * du paquet de tokens dont elle dépend.
 */
export * from "./icons.js";
export * from "./factories.js";
