{
  "name": "datagraph-workspace",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/defsquare/datagraph.git"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "bench": "pnpm --filter @defsquare/datagraph-core bench"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
