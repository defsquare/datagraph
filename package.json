{
  "name": "data-graph-workspace",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/defsquare/data-graph"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "bench": "pnpm --filter @defsquare/data-graph-core bench"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
