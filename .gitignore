node_modules/
dist/
*.log
.DS_Store
.idea/
.claude/worktrees/
test-results/
playwright-report/
# Playwright MCP scratch: screenshots and page snapshots. The *.log rule above
# already hid its console dumps, which is why the .png and .yml never got noticed.
.playwright-mcp/
apps/demo/src-tauri/target/
apps/demo/src-tauri/gen/

# Release config (tokens, local paths): see bin/release.env.example.
bin/release.env

# Working datasets (client schemas, generator output): local only, never committed.
datasets/
